name: Example Workflow

on: [push]

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run your custom action
        uses: polatengin/repo_TO_BE_DELETED@v2.0
        with:
          name: 'Engin'
